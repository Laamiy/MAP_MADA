import type { MapTheme } from "@/types/map.theme.types";

//TODO : might wanna make this a more useful function later on.
export function onThemeToggle(theme: MapTheme): void
{
  console.log(`current theme :${theme}`)
}
